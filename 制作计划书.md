# 「口算小达人」制作计划书

> 项目：小学二年级 100 以内加减法随机出题器（Windows 桌面程序）
> 版本：v1.0（规格锁定版）
> 日期：2026-09-24

---

## 一、项目概述

为小学二年级学生制作一个 Windows 桌面口算练习程序：

- 双击 exe 直接运行，无需安装任何环境
- 每次随机出题，一次一题，键盘输入答案
- 两种模式：**练习模式**（20 题、不限时）、**测验模式**（100 题、限时 20 分钟）
- 答完自动统计对错、用时，支持错题回顾、错题重练、历史成绩查看
- 界面简体中文、大字号、明亮配色，二年级学生可独立操作

## 二、需求规格（已与用户逐项确认锁定）

### 2.1 出题规则

| 项目 | 规格 |
|---|---|
| 题型配比 | 两位数±一位数占 **20%**（练习 4 道 / 测验 20 道）；两位数±两位数占 **80%**（练习 16 道 / 测验 80 道）；题目顺序打乱 |
| 进位退位 | 不刻意控制比例，由随机自然产生（统计上约各半）；引擎需保证每轮中既出现进退位题也出现不进位题 |
| 数值约束 | 两位数取值 10–99；一位数取值 1–9；加法结果 ≤ 100；减法结果 ≥ 0（大减小，不出负数） |
| 查重 | 同一轮内无重复题目；加法交换律视为同一题（如 47+35 与 35+47 不会同时出现） |
| 答案范围 | 输入答案最多 3 位数 |
| 随机性 | 每次进入模式重新随机生成，两轮题目不同 |

### 2.2 两种模式

| 项目 | 练习模式 | 测验模式 |
|---|---|---|
| 题量 | 20 题 | 100 题 |
| 时间 | 不限时；第 1 题出现起**正计时**，第 20 题提交/跳过后停止 | 第 1 题出现起 **20:00 倒计时**；剩余 1 分钟变红提醒；归零锁定界面自动交卷，未答题计为错误 |
| 反馈 | 不即时反馈对错，内部记录；答完 20 题统一提示"答对 X 题" | 同左 |
| 界面 | 一次一题，显示题号进度（第 X 题/共 Y 题）、进度条、计时 | 同左 |

### 2.3 交互规则

1. 输入框只接受数字键，其他字符（字母、符号、空格、小数点）自动拦截，最多 3 位；支持小键盘数字
2. 答案非空：按**回车**（含小键盘 Enter）或点【下一题】→ 切换下一题
3. 答案为空：按回车、点【下一题】均无效，给出提示"请先输入答案，或点击跳过"；只能点【跳过】才能切题
4. **跳过的题目计为答错**；正确率 = 答对数 ÷ 总题数
5. 每题切换后：输入框自动清空并**自动获得焦点**，孩子无需用鼠标点击即可直接输入
6. 答案按整数判定：允许前导零（如输入 "082" 按 82 判定）
7. 答题中关闭窗口：弹确认框"确定退出吗？本轮成绩不会保存"（**仅答题进行中弹确认**，其他页面直接关闭）
8. 测验模式时间到：锁定输入（当前正在输入但未提交的题视为未答、计为错误），自动交卷进入成绩页
9. 错题重练时沿用练习模式规则（不限时、不即时反馈）

### 2.4 成绩与错题

- **成绩页**：大字显示"答对数/总数"、答对题数、正确率、用时；测验模式注明"限时内完成"或"时间到"
- **鼓励语分档**：≥90%「太棒了！五星上将！」；70–89%「真不错，继续加油！」；<70%「别灰心，再来一轮试试！」
- **错题回顾**：成绩页按钮【看看错题】；列出答错 + 跳过的题目、孩子当时的答案（红）、正确答案（绿）；跳过题显示"跳过"字样；全对时显示"全对，没有错题！"+ 庆祝样式
- **错题重练**：错题页【错题重练】按钮，用当前错题本重新组卷；**错题本动态更新**——重练中答对的题移出错题本，下一轮重练只用仍答错的题，循环直至全对；**错题重练成绩不计入历史成绩**（避免 1/1、2/2 之类小样本污染统计）；重练结束可【返回错题】或【返回菜单】
- **历史成绩**：主菜单【历史成绩】按钮，软件内显示最近 50 次（日期、模式、成绩、用时）；每次成绩自动追加保存到 exe 同目录 `口算小达人-历史成绩.txt`（UTF-8 编码，记事本可直接打开）
  - txt 格式：首行为标题说明，之后每行一条记录：
    `2026-09-24 20:15:33 | 练习模式 | 答对 18/20 | 正确率 90% | 用时 6分23秒`
    测验模式时间到时在末尾追加 `| 时间到`

### 2.5 限时依据（调研结论）

- 课标第一学段：100 以内加减法口算 3–4 题/分钟（底线）
- 常见达标标准：8–10 题/分钟（合格），100 题合格线约 10–12 分钟
- 全国调研平均约 4.8 秒/题；考虑键盘输入比口算/手写慢（二年级打字不熟，每题多 3–5 秒），实际约 8–12 秒/题，100 题约 13–20 分钟
- **结论：限时 20 分钟**——中等速度够用且略有紧迫感，不竞赛化

## 三、界面设计（共 5 个界面）

设计草图见 `设计草图/` 文件夹（SVG 文件，双击浏览器可看）。

### 3.1 主菜单（界面①）

- 窗口约 900×600，标题「口算小达人」
- 大标题「100 以内加减法」+ 副标题「小学二年级口算练习」
- 三个按钮分主次：**练习模式**（蓝色大按钮，标注"20道题 · 不限时间"）、**测验模式**（橙色大按钮，标注"100道题 · 限时20分钟"）为主按钮；**历史成绩**（灰色小按钮）为次级入口，视觉层级低一档
- 底部提示「选一个开始吧！」

### 3.2 答题界面（界面②）

- 顶部：左「第 X 题 / 共 Y 题」、右计时器（时钟图标 + 05:23）
- 顶部下方：进度条（蓝色填充）
- 中央：超大算式「47 + 35 =」+ 答案输入框（大字号、光标闪烁、蓝色边框）
- 输入框下方提示「按回车键进入下一题」
- 按钮区：【下一题】（绿）、【跳过】（红边框）
- 答案为空时按回车/点下一题 → 输入框边框变红 + 下方红字提示"请先输入答案，或点击跳过"，输入任意数字后自动恢复
- 算式与输入框置于浅色卡片中，视觉聚焦；按钮主次分明：【下一题】大且醒目，【跳过】小一号并与【下一题】拉开间距，防止误点
- 测验模式时间到：顶部弹出"时间到！"横幅，随即进入成绩页

### 3.3 成绩界面（界面③）

- 大字「做完啦！」+ 星星奖励：**星星数量与正确率挂钩——≥90% 三颗、70–89% 两颗、<70% 一颗**（金色 ★ 符号字符绘制，兼容性好）
- 超大成绩「18 / 20」
- 「答对 18 题」（绿）、「正确率 90%」、「用时 6分23秒」
- 按钮区：【再来一轮】（蓝）、【返回菜单】（灰）；
- 【看看错题】按钮进入错题回顾

### 3.4 错题回顾界面（界面④）

- 标题「看看错题」
- 汇总行「共 X 道错题（答错 X · 跳过 X）」
- 表格三列：题目（标注原题号，如"第 3 题 47 + 35"）/ 你的答案（红，跳过显示"跳过"）/ 正确答案（绿）；错题多时可滚动（测验模式可能错几十道）
- 按钮：【错题重练】（橙）、【返回】（灰）
- 全对时：庆祝样式 +「全对，没有错题！」

### 3.5 历史成绩界面（界面⑤）

- 标题「历史成绩」
- 顶部汇总行：「共练习 X 次 · 平均正确率 Y%」（家长一眼看趋势）
- 表格四列：日期 / 模式（练习蓝、测验橙）/ 成绩 / 用时
- 交替行底色；最新在上；软件内显示最近 50 次，可滚动
- 底部注明完整记录保存于软件旁 txt 文件
- 按钮：【返回菜单】

### 3.6 视觉规范

- 配色：明亮浅色系；主色蓝 #378ADD、成功绿 #639922、测验橙 #EF9F27、错误红 #A32D2D、背景米白 #F1EFE8、正文深灰 #444441、标题深紫 #26215C
- 字号：算式特大（≥48px）、按钮和成绩大字（20–32px）、辅助文字 ≥13px
- 字体：系统默认中文字体（微软雅黑）
- 窗口：固定大小、屏幕居中、不可拉伸
- 实现注记：tkinter 原生按钮为扁平矩形，以纯色块 + 留白近似草图的圆角效果；装饰图标统一用 ★ 等通用符号字符，避免 emoji 在部分系统显示为方框

## 四、技术方案

| 项 | 方案 |
|---|---|
| 语言 | Python 3.13 |
| GUI | tkinter（Python 官方内置，零第三方界面依赖） |
| 打包 | PyInstaller 单文件模式（--onefile --windowed） |
| 数据存储 | 纯文本 txt（UTF-8），与 exe 同目录，免依赖 |
| 运行要求 | Windows 7 及以上，无需安装 Python |

## 五、制作步骤（每步完成并验收后进入下一步）

| 步骤 | 内容 | 验收标准 |
|---|---|---|
| 1 | 出题引擎（配比、约束校验、交换查重） | 自动生成 1 万题跑校验，全部满足约束、无重复、配比正确 |
| 2 | 主菜单界面 | 三按钮显示正常、可响应 |
| 3 | 答题界面 + 全部交互规则 | 输入过滤、空答案拦截、跳过、切题、进度显示全部符合 2.3 规范 |
| 4 | 计时器 | 正计时精确到秒；倒计时归零自动交卷；剩余 1 分钟变红 |
| 5 | 成绩统计 + 成绩页 | 数据正确、鼓励语分档正确、自动保存历史 |
| 6 | 错题回顾 + 错题重练 | 列表内容正确；重练可循环至全对 |
| 7 | 历史成绩界面 | 界面数据与 txt 一致 |
| 8 | 视觉优化 | 与设计草图一致、大字号清晰、窗口居中固定、退出确认 |
| 9 | 全流程自测 | 下方 14 条清单逐条通过 |
| 10 | PyInstaller 打包（配程序图标） | 双击 exe 可启动、完整走通两种模式 |
| 11 | 交付 | exe + 源码 + 计划书交付 |

### 自测清单（14 条）

1. 出题 1 万题自动校验全部通过、无重复、配比 20:80
2. 空答案按回车 → 不切题、有提示
3. 空答案点【下一题】→ 不切题、有提示
4. 空答案点【跳过】→ 切题、内部记录为错
5. 有答案按回车 / 点【下一题】→ 切题、记录对错
6. 输入字母、符号、空格、小数点 → 全部拦截；小键盘数字与回车可用
7. 练习模式答完 20 题 → 自动出成绩，用时准确
8. 测验模式倒计时归零 → 锁定界面自动交卷出成绩
9. 测验模式提前答完 → 立即出成绩
10. 跳过题计入错误、正确率计算正确
11. 错题列表：题目、孩子答案、正确答案与实际一致
12. 错题重练可循环直至全对；重练成绩不写入历史成绩
13. 时间到时正在输入的题按未答计错、界面立即锁定
14. exe 双击运行无异常，两种模式完整走通

## 六、文件结构（项目文件夹）

```
口算小达人/
├── 制作计划书.md          ← 本文档
├── 设计草图/
│   ├── 界面1-3-模式选择-答题-成绩.svg
│   └── 界面4-5-错题回顾-历史成绩.svg
├── src/
│   └── 口算小达人.py       ← 程序源码（步骤 1 起创建）
├── 口算小达人.exe          ← 打包产物（步骤 10 生成）
└── 口算小达人-历史成绩.txt  ← 运行时自动生成
```

## 七、风险与对策

| 风险 | 对策 |
|---|---|
| PyInstaller 打包 exe 被个别杀毒软件误报 | 常见现象，非病毒；引导用户添加信任 |
| 不同 DPI 缩放下界面模糊/变形 | 使用 tkinter 高 DPI 感知声明，自测 100%/125%/150% 缩放 |
| txt 历史文件被手动删除 | 程序容错：文件不存在时自动重建，不影响使用 |
| 孩子电脑开着中文输入法 | 数字键不受中文输入法影响（直接上屏）；自测时实测验证一次 |
| exe 文件名/所在路径含中文 | PyInstaller 支持中文；打包后在中文路径下自测一次 |

---

**规格状态：全部锁定（2026-09-24 与用户逐项确认）**
**修订记录：2026-09-24 补充完善 8 处——切题后自动聚焦、小键盘支持、前导零判定、关闭确认仅限答题中、时间到未提交题计错、错题重练动态错题本且不计入历史、错题/历史表格滚动、txt 记录格式定义**
**修订记录 2：2026-09-24 UI 升级 8 处——成绩星星与正确率挂钩、算式卡片聚焦、空答案红框提示样式、按钮主次防误点、时间到横幅、历史成绩汇总行、错题标注原题号、tkinter 实现注记**

---

## 八、进度记录

| 步骤 | 状态 | 验收结果 |
|---|---|---|
| 1 出题引擎 | ✅ 完成 | 1 万题自动校验全部通过（约束/查重/进退位混合/配比20:80），加减比例 50.3% |
| 2 主菜单 | ✅ 完成 | 三按钮正常，截图核对与设计稿一致 |
| 3 答题界面+交互 | ✅ 完成 | 空答案回车与点下一题不切题并有红框提示、跳过可切题并记录、输入过滤、前导零判定 |
| 4 计时器 | ✅ 完成 | 练习正计时；测验 20 分钟倒计时，剩余 1 分钟变红，归零锁定并自动交卷 |
| 5 成绩统计+成绩页 | ✅ 完成 | 星星分档（≥90%三星/70-89%两星/<70%一星）、正确率、用时、自动写历史 |
| 6 错题回顾+重练 | ✅ 完成 | 列表含题号/你的答案/正确答案；重练用动态错题本，答对移出，循环至全对；不计入历史 |
| 7 历史成绩 | ✅ 完成 | 软件内最近 50 次 + 汇总行（共X次·平均正确率Y%），与 txt 记录一致 |
| 8 视觉优化 | ✅ 完成 | 大字号、明亮配色、窗口居中固定、退出确认；7 个页面截图目视核对通过 |
| 9 全流程自测 | ✅ 完成 | 自检脚本 17/17 通过（覆盖下方 14 条清单全部条目） |
| 10 PyInstaller 打包 | ✅ 完成 | 单文件 exe 11.6 MB，含蓝底金星图标；打包版自检 17/17 通过；双击启动窗口正常 |
| 11 交付 | ✅ 完成 | `口算小达人.exe` + `src/口算小达人.py` + 计划书 + 设计草图；代码已推送 GitHub |

**环境说明**：源码运行与打包使用系统 Python 3.14.4（含 tkinter 8.6）；打包在独立虚拟环境
`~/.workbuddy/binaries/python/envs/py314tk`（PyInstaller 6.22.3）中完成。

**打包命令**：
```
pyinstaller --onefile --windowed --icon "assets/icon.ico" --name 口算小达人 \
  --distpath . --workpath build --specpath build src/口算小达人.py
```

**已知提示**：PyInstaller 打包的 exe 可能被杀毒软件误报，添加信任即可。
**可选的后续增强**：将 exe 上传到 GitHub Releases 供下载；增加连对/勋章激励；支持家长自定义题量与限时。

---

## 九、v1.1 用户与权限升级（2026-09-24 晚，规格已与用户确认）

### 9.1 背景

v1.0 试用后提出 4 项需求：历史记录无法删除；需区分使用者；用户可记忆可切换；答题中途关闭能否保留记录。

### 9.2 用户体系与权限矩阵（锁定）

| 功能 | 孩子 | 家长 |
|---|---|---|
| 练习 / 测验 / 错题重练 | ✓ | ✓ |
| 切换用户（仅主菜单出现切换入口；答题中不可切换） | ✓ 一键切换 | ✓ 需输密码 |
| 查看历史成绩 | 仅自己 | 任意用户（页内可切换查看对象） |
| 删除单条记录 / 清空记录 | ✗（按钮不显示） | ✓ |
| 用户管理（添加/删除用户、修改密码） | ✗ | ✓ |

- **密码**：4–6 位数字 PIN；**家长首次运行时自行设置**（加盐哈希存储，不明文）；忘记密码可删除 `口算小达人-用户.json` 重置（登录框下方注明）
- **用户记忆**：记住用户列表与上次登录用户，重启自动保持

### 9.3 数据存储（锁定）

```
口算小达人-用户.json            ← 用户列表（姓名/角色/密码哈希/盐）、当前用户
口算小达人-历史成绩-<用户名>.txt ← 每用户一个历史文件（格式同 v1.0，记事本可读）
```

- 用户名中文件系统非法字符（`\/:*?"<>|`）入库前替换
- **旧数据处理：清空重来**——v1.1 首次启动时删除 v1.0 的 `口算小达人-历史成绩.txt`

### 9.4 界面变化（锁定）

设计草图见 `设计草图/界面v1.1-1~3*.svg`（8 个界面/弹窗，双击浏览器可看）：

- **主菜单**：顶部用户条「当前：王二（学生）」+【切换用户】；点孩子直接切换，点家长弹密码框
- **切换用户面板**：用户列表，孩子标注"一键切换"（绿）、家长标注"需密码"（橙），头像按角色着色
- **家长密码框**：含"忘记密码删除用户配置文件即可重置"的找回指引；输错红色提示
- **历史成绩页**：孩子无删除按钮；家长可逐条【删】+【清空记录】，顶部可切换查看对象
- **用户管理页**（仅家长）：添加/删除用户、修改密码；保护规则——不能删除自己、至少保留 1 名家长、至少保留 1 个用户；删除用户同时删除其历史文件（需确认）
- **首次运行向导**：无用户时自动引导：创建家长（姓名+设密码）→ 创建孩子（姓名）→ 完成；姓名 1–10 字且不重复

### 9.5 答题中途关闭（锁定）

- 关闭确认框由两选改**三选**：【保存并退出】【直接退出】【继续答题】
- 保存规则：**已答 ≥1 题才可保存**；记录该轮已答部分，行尾追加标记 `| 中途退出`
- **准确率按已答题数计算**：如答 12 题对 10 题 → `答对 10/12 | 正确率 83% | 用时 X分Y秒 | 中途退出`
- 保存后进入成绩页展示部分成绩（注明"中途退出，按已答 X 题计算"），可返回菜单；已答 0 题则只能【直接退出】或【继续答题】

### 9.6 实施步骤（增量，每步验收后推进）

| 步骤 | 内容 |
|---|---|
| v1 | 数据层：users.json 读写、每用户历史文件路径、v1.0 旧数据清空迁移 |
| v2 | 首次运行向导（建家长含密码、建孩子） |
| v3 | 主菜单用户栏 + 切换面板 + 家长密码框 |
| v4 | 历史页：按用户隔离、查看对象切换、家长删除/清空（txt 同步） |
| v5 | 用户管理页（添加/删除/改密 + 保护规则） |
| v6 | 中断处理：三选对话框 + 部分成绩保存（中途退出标记、准确率按已答） |
| v7 | 自检扩展（权限/隔离/删除/中断/迁移）+ 界面截图核对 |
| v8 | 重新打包 exe 交付 v1.1 |

### 9.7 自检新增用例

1. 首次向导完整走通，users.json 正确、当前用户正确
2. 家长输错密码不能切换；孩子一键切换成功；切换后历史与错题本隔离
3. 孩子登录时历史页无删除按钮；家长有且可删单条（txt 同步少一行）
4. 家长清空记录 → txt 重置为表头
5. 添加/删除用户同步落盘；删除用户连带删除其历史文件；不能删自己/最后一名家长
6. 修改密码后旧密码失效、新密码生效
7. 答 3 题后保存退出 → 记录 `答对 x/3 | 中途退出`，正确率按 3 题计
8. v1.0 旧历史文件在首次启动时被清空
9. 打包版 exe 上述全部用例通过
